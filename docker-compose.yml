
services:
  render:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APPNAME: render
    networks:
      - render-network
  
  cassandra:
    image: cassandra:4.1
    networks:
      - render-network

networks:
  render-network:
    driver: bridge
